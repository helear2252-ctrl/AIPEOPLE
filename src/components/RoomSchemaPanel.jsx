import {useState} from 'react';import {useInteriorAgentStore} from '../store/useInteriorAgentStore';
export default function RoomSchemaPanel(){const [open,setOpen]=useState(false);const {isSchemaReady,roomSchema}=useInteriorAgentStore();return <section className={`schema-strip ${isSchemaReady?'ready':''}`}><button onClick={()=>setOpen(v=>!v)}><span>{isSchemaReady?'✓':'04'}</span><div><small>CORE DATA FORMAT</small><b>{isSchemaReady?'Room Schema generated':'Waiting for schema conversion'}</b></div><i>{open?'−':'+'}</i></button>{open&&<pre>{JSON.stringify(roomSchema,null,2)}</pre>}</section>}
