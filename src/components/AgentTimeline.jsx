import {agentRoster} from '../data/agentSteps';import {useInteriorAgentStore} from '../store/useInteriorAgentStore';
export default function AgentTimeline(){const agents=useInteriorAgentStore(s=>s.agents);return <section className="panel agent-panel"><div className="panel-mini-heading"><span>AGENT NETWORK</span><i>09 modules</i></div><div className="agent-list">{agentRoster.map((name,index)=>{const state=agents[name]||{status:'idle'};return <div className={`agent-row ${state.status}`} key={name}><span className="agent-index">{String(index+1).padStart(2,'0')}</span><div><b>{name}</b><small>{state.status}</small></div><i>{state.status==='completed'?'✓':state.status==='running'?'···':'·'}</i></div>})}</div></section>}
