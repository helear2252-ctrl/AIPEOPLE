import {useInteriorAgentStore} from '../store/useInteriorAgentStore';
export default function MaterialSwitcher(){const {materialTheme,setMaterialTheme,lightMode,setLightMode,isSchemaReady,appendLog}=useInteriorAgentStore();const setTheme=v=>{setMaterialTheme(v);appendLog(`Material Agent regenerated palette: ${v}.`,'Material Agent')};return <div className="viewer-controls"><div className="segmented">{['warm minimal','minimal white','dark luxury'].map(v=><button disabled={!isSchemaReady} className={materialTheme===v?'active':''} key={v} onClick={()=>setTheme(v)}>{v}</button>)}</div><div className="light-toggle"><button className={lightMode==='day'?'active':''} onClick={()=>setLightMode('day')}>☀ Day</button><button className={lightMode==='night'?'active':''} onClick={()=>setLightMode('night')}>◐ Night</button></div></div>}
